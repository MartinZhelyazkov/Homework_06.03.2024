server:
  port: 8080
spring:
  application:
    name: hibernateHomework
  datasource:
    url: jdbc:mysql://localhost:3306/hibernateHomework?createDatabaseIfNotExist=true
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format-sql: true
